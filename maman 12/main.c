#include "data.h"
/*
  the main  function only calls for extern function
  i used an external text file to test my function named as array example.txt
*/

int main()
{

fill_array(NAMES);
get_name(temp_str);
  return 0;
}
